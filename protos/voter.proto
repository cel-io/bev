syntax = "proto3";

message Voter{
    string voter_id = 1;
    uint64 created_at = 2;

    enum Type {
        VOTER = 0;
        ADMIN = 1;
        SUPERADMIN = 2;
    }

    Type type = 3;
}

message VoterContainer {
    repeated Voter entries = 1;
}
